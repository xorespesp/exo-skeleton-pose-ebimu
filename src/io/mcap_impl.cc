// MCAP is a header-only library: 
// exactly one translation unit must define MCAP_IMPLEMENTATION to emit its symbols. 
// Everywhere else the headers are declarations only.
#define MCAP_IMPLEMENTATION
#include <mcap/reader.hpp>
#include <mcap/writer.hpp>
